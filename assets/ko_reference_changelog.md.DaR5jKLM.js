import{_ as i,o as s,c as r,j as o,a,a3 as t,t as n}from"./chunks/framework.DzbxBm4o.js";const k=JSON.parse('{"title":"변경 내역","description":"chki18n의 모든 릴리스를 최신순으로 정리했습니다. 각 패키지는 독립적으로 버전을 관리하므로, 한쪽의 릴리스가 다른 쪽의 릴리스를 의미하지는 않습니다.","frontmatter":{"title":"변경 내역","order":3},"headers":[],"relativePath":"ko/reference/changelog.md","filePath":"ko/reference/changelog.md","lastUpdated":null}'),c={name:"ko/reference/changelog.md"},l={class:"chki18n-lang","data-lang":"js"},h={class:"chki18n-lang","data-lang":"dart"},u={class:"chki18n-lang","data-lang":"py"};function p(d,e,g,f,m,w){return s(),r("div",null,[e[30]||(e[30]=o("h1",{id:"변경-내역",tabindex:"-1"},[a("변경 내역 "),o("a",{class:"header-anchor",href:"#변경-내역","aria-label":'Permalink to "변경 내역"'},"​")],-1)),e[31]||(e[31]=o("p",null,"chki18n의 모든 릴리스를 최신순으로 정리했습니다. 각 패키지는 독립적으로 버전을 관리하므로, 한쪽의 릴리스가 다른 쪽의 릴리스를 의미하지는 않습니다.",-1)),o("div",l,[e[5]||(e[5]=o("p",null,[a("npm의 "),o("code",null,"chki18n"),a(" 패키지입니다.")],-1)),e[6]||(e[6]=o("h2",{id:"_1-2-0-2026-09-05",tabindex:"-1"},[a("1.2.0 (2026-09-05) "),o("a",{class:"header-anchor",href:"#_1-2-0-2026-09-05","aria-label":'Permalink to "1.2.0 (2026-09-05)"'},"​")],-1)),e[7]||(e[7]=o("blockquote",null,[o("p",null,"A scan can be pointed at an application's root rather than at a folder of locales. It skips the configuration and lock files that root is full of, an exclude can name a path rather than a bare directory name, and it reports which interpolation delimiters the files it read appear to use.")],-1)),e[8]||(e[8]=o("h3",{id:"added",tabindex:"-1"},[a("Added "),o("a",{class:"header-anchor",href:"#added","aria-label":'Permalink to "Added"'},"​")],-1)),o("ul",null,[e[3]||(e[3]=t("<li><code>excludeFiles</code> names the files a scan never reads, as patterns where <code>*</code> stands for any run of characters and case is ignored. It defaults to the configuration and lock files no project keeps translations in — <code>package.json</code>, <code>tsconfig.json</code>, <code>tsconfig.*.json</code>, <code>eslintrc.json</code>, <code>*-lock.json</code>, <code>*-config.json</code> and <code>*.config.json</code> — which a scan of an application root used to read and parse in full on every run. The default list is exported as <code>DEFAULT_EXCLUDE_FILES</code>, and <code>--exclude-files</code> is the flag</li>",1)),o("li",null,[e[0]||(e[0]=o("code",null,"detectInterpolationDelimiters",-1)),e[1]||(e[1]=a(" guesses which delimiters a text writes its interpolation keys with: ",-1)),o("code",null,n(d.name),1),e[2]||(e[2]=t(" before <code>{name}</code>, and <code>[[ ]]</code>, <code>(( ))</code> and <code>&lt;&lt; &gt;&gt;</code> after them. <code>Chki18nScanResult.detectedInterpolation</code> reports what a whole scan saw, which <code>loadTranslations</code> carries as <code>session.detectedInterpolation</code>. It is a suggestion for a project being set up, never what the run compared with — that stays <code>interpolationPrefix</code>. The pairs it knows are exported in order as <code>INTERPOLATION_DELIMITERS</code>",18))]),e[4]||(e[4]=o("li",null,[o("code",null,"createPathExcluder"),a(" and "),o("code",null,"createFileExcluder"),a(" build the two tests a scan applies, so an application that shows a user which folders are excluded can ask the same question the scan asks")],-1))]),e[9]||(e[9]=t('<h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><code>exclude</code> accepts a path as well as a name. One segment still names a directory at any depth, so <code>node_modules</code> means every <code>node_modules</code> there is; an entry with a separator names a path from the scanned root, so <code>src/legacy</code> excludes that folder and everything under it without touching a <code>legacy</code> belonging to something else</li><li>Both exclude lists apply to the source tree <code>source</code> names as well as to the translation directory</li></ul><h2 id="_1-1-0-2026-09-04" tabindex="-1">1.1.0 (2026-09-04) <a class="header-anchor" href="#_1-1-0-2026-09-04" aria-label="Permalink to &quot;1.1.0 (2026-09-04)&quot;">​</a></h2><blockquote><p>The target language is checked too. Everything else is compared against it, so it sat outside every check, and a mistake typed into the source language stayed there however often the files were checked.</p></blockquote><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li>The target language is now checked by everything that reads one value on its own: <code>EMPTY_VALUE</code>, <code>SURROUNDING_WHITESPACE</code>, <code>INVISIBLE_CHARACTER</code>, <code>INVALID_VALUE_TYPE</code> and <code>UNTRANSLATED_SCRIPT</code>. It was skipped outright before, so an empty string, a trailing space or a zero width character in <code>en.json</code> was never reported. The comparison checks still say nothing about it, since it is what they compare against. None of the five reports at <code>error</code>, so a run that passed still passes; switch one off with <code>ignoreChecks</code> the way you would for any other language</li><li>An issue about the target language&#39;s own value carries no <code>targetValue</code>. What a report quotes beside a finding is the value it would be compared to, and for the target language that is the value already shown</li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li>The <code>github</code> reporter writes the annotation&#39;s <code>file=</code> path with forward slashes on every platform. On a Windows runner it wrote the platform&#39;s own separator, which GitHub matches against nothing, so the annotation silently attached to no file</li></ul><h2 id="_1-0-0-2026-09-03" tabindex="-1">1.0.0 (2026-09-03) <a class="header-anchor" href="#_1-0-0-2026-09-03" aria-label="Permalink to &quot;1.0.0 (2026-09-03)&quot;">​</a></h2><blockquote><p>The first stable release. The checks no longer depend on the file system, so they run against translations an application already holds in memory as well as against a folder of files; the report was rebuilt around what a reader is looking for, and can be written as JSON, Markdown or GitHub annotations; and the comparison grew to twenty-five checks.</p></blockquote><h3 id="breaking-changes" tabindex="-1">Breaking changes <a class="header-anchor" href="#breaking-changes" aria-label="Permalink to &quot;Breaking changes&quot;">​</a></h3><ul><li>A project that passed on <code>0.3.0</code> can fail on this release. Four of the checks added report at <code>error</code> — <code>NO_LOCALE</code>, <code>DUPLICATE_KEY</code>, <code>EXTRA_INTERPOLATION_KEY</code> and <code>INTERPOLATION_COUNT</code> — and <code>EMPTY_VALUE</code> and <code>DUMMY_KEY</code> were fixed to report at all. Everything else added reports at <code>warn</code> or <code>info</code>, which never fails a run. Switch off what a project does not want with <code>ignoreChecks</code>, or <code>--ignore-checks</code> on the command line</li><li>The CLI report was rebuilt around what the reader is looking for: a heading block naming what was scanned, one section per language with its own tally, each check&#39;s meaning printed once above its findings, and a summary that counts the axis the sections did not use. Columns are laid out by display width, so a Korean, Japanese or Chinese value no longer pushes the ones beside it out of line. Nothing should read the text of that report — anything that used to parse standard output wants <code>--reporter json</code></li><li><code>--no-info</code> drops the heading block and the summary rather than only the progress lines, and <code>--debug</code> writes to standard error so a report piped out of standard output stays parseable</li><li>The CLI moved to its own entry point (<code>dist/cli.js</code>), which the <code>chki18n</code> binary points at. Importing the module has no side effect and never writes to the console or exits the process, so an import that ran the CLI has to call <code>checkTranslationFiles</code> instead</li><li>The result now carries every issue, whether the run passed or failed, as a flat <code>issues</code> list plus <code>issuesByCode</code> and a <code>summary</code> with per-level, per-code, per-locale and per-group counts. Each issue has its own <code>level</code>, <code>message</code> and originating <code>file</code>. Code that read the old shape has to move to these fields</li></ul><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><code>analyzeTranslations</code> compares translations passed in directly, with no file system work</li><li><code>createAnalyzer</code> returns a reusable analyzer whose <code>checkEntry</code> re-checks a single key</li><li><code>loadTranslations</code> reads a directory once and returns a session that holds the parsed translations: <code>analyze</code>, <code>checkKey</code>, <code>get</code>, <code>set</code>, <code>remove</code>, <code>keys</code>, <code>translations</code> and <code>reload</code> all work on what is already in memory. <code>createSession</code> is the same for translations passed in directly</li><li><code>EXTRA_INTERPOLATION_KEY</code>, <code>SURROUNDING_WHITESPACE</code>, <code>MISSING_NUMBER</code> and <code>INVALID_VALUE_TYPE</code> checks</li><li><code>NO_LOCALE</code>, for a group of files that holds nothing for a language the other groups have. Until now a translation file nobody created dropped out of the comparison entirely and the run passed</li><li><code>INTERPOLATION_COUNT</code>, for a placeholder used a different number of times than the target language uses it. The two interpolation checks compare which placeholders a value has, so <code>{name} invited {name}</code> translated with one <code>{name}</code> passed both</li><li><code>TAG_MISMATCH</code>, for markup a translation dropped, added or left unbalanced. Tags are counted rather than looked for, and read case-insensitively</li><li><code>UNTRANSLATED_SCRIPT</code>, for a value holding no character of the script its language is written in. <code>NOT_TRANSLATED_VALUE</code> only catches a translation identical to the original, so <code>Hello</code> becoming <code>Hello!</code> passed it</li><li><code>INVISIBLE_CHARACTER</code>, for a zero width space, a byte order mark, a bidirectional control or a non-breaking space</li><li><code>NUMBER_MISMATCH</code>, for a number the translation changed rather than dropped. <code>MISSING_NUMBER</code> only asks whether any digits survived</li><li><code>INCONSISTENT_VALUE</code>, for two keys sharing one target language string that a locale translates two different ways. <code>DUPLICATE_VALUE</code> asks whether one locale repeats itself; this asks the opposite</li><li><code>KEY_NAMING</code> and <code>KEY_DEPTH</code>, for the shape of a key rather than what it translates to. Both wait for <code>keyCase</code> and <code>maxKeyDepth</code> to say what the project wants, and are judged once per key rather than once per locale</li><li><code>keyCase</code> and <code>maxKeyDepth</code>, the options those two compare against. <code>keyCase</code> accepts the plural and context suffixes an i18n library appends, whatever case the project uses</li><li><code>UNDEFINED_KEY</code>, the reverse of <code>UNUSED_KEY</code> and the more serious of the two: the scanned source calls for a key and no language file defines it. Reads the calls it finds under <code>source</code>, and lets through a key built at run time, one reached through a bound prefix, and a plural key asked for by its base</li><li><code>translateFunctions</code>, the names a translation call goes by. The default covers i18next, react-i18next and vue-i18n</li><li><code>NO_PLURAL_FORM</code>, for a plural key missing a form its language needs. Which forms a language needs is a fact about the language rather than about the original: English writes two, Russian four, Korean one. The older i18next pairing of a bare key with <code>_plural</code> is left as ordinary keys, and a language the table does not cover is never judged</li><li><code>SUSPICIOUS_LENGTH</code>, reported at <code>info</code>, for a value far longer or shorter than the one it translates. Waits for <code>lengthRatio</code>, and measures columns rather than characters so a Korean or Japanese value is not short by default</li><li><code>lengthRatio</code>, the option it compares against</li><li><code>checkKeyShape</code> and <code>displayWidth</code> are exported, so an editor can judge a key the user is still typing and lay a value out the way the report does</li><li><code>extractTags</code>, <code>extractNumbers</code>, <code>findInvisibleCharacter</code>, <code>scriptOfLocale</code> and <code>hasTranslatableText</code> are exported, so an editor can run the same measurements on a value it is holding</li><li><code>DUPLICATE_KEY</code>, which catches a key defined twice — both the literal kind (<code>{&quot;a&quot;: 1, &quot;a&quot;: 2}</code>, which <code>JSON.parse</code> resolves silently) and the kind where a nested key and a dotted one flatten onto each other</li><li><code>UNUSED_KEY</code>, reported at <code>info</code>, for keys nothing in a <code>source</code> directory appears to reference. An application that has already worked this out can pass its own answer as <code>unusedKeys</code> instead</li><li><code>source</code>, the directory the unused-key scan searches</li><li>Support for the folder-per-locale (<code>en/common.json</code>) and single-file-per-project (<code>{ &quot;en&quot;: ... }</code>) layouts, alongside the existing one-file-per-locale layout. Files holding the same keys are compared as a group, so several translation files no longer share one pile of keys</li><li><code>Chki18nInput</code> accepts <code>issues</code> and <code>fileFormat</code>, so whatever produced the input can report its own problems into the same result</li><li><code>chki18n/core</code>, a subpath that exports the comparison engine on its own. It imports no Node built-in, so it bundles for a browser or an editor&#39;s renderer process</li><li><code>checks</code>, <code>ignoreChecks</code>, <code>levels</code>, <code>format</code>, <code>exclude</code> and the interpolation delimiter options, available both as CLI flags and as JavaScript options</li><li><code>--help</code> and <code>--version</code></li><li><code>reporter</code>, which decides the shape of the report: <code>pretty</code> for a terminal, <code>list</code> for one line per issue, <code>json</code> for another program to read, <code>markdown</code> for a table, and <code>github</code> for the workflow commands GitHub Actions turns into annotations on the translation files themselves. Everything but <code>pretty</code> prints the report alone, with no banner, so it can be piped straight into something else</li><li><code>groupBy</code>, the axis the report groups its issues by: <code>locale</code> (the default), <code>code</code>, <code>group</code>, <code>file</code> or <code>none</code></li><li><code>output</code>, a file the report is written to as well as the terminal. The extension picks the format, an explicit <code>reporter</code> overrides it, missing directories are created, and a write that fails is reported as an error and fails the run</li><li><code>color</code>, and <code>--no-color</code> with it. A file written by <code>output</code> is never coloured</li><li><code>width</code>, the column count the report is laid out to. Without it the terminal&#39;s own width is used, then <code>COLUMNS</code>, which is where a CI runner reports its log width</li><li><code>formatResult</code> and <code>groupIssues</code> are exported, so an application can render a result the way the CLI does, along with <code>displayWidth</code>, <code>padTo</code> and <code>truncate</code> for laying out columns of its own</li></ul><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><code>CHECK_META</code> describes each check&#39;s severity and wording, so a user interface does not have to hard-code them</li><li>A description that does not fit the width wraps onto the next line instead of being cut short, so a narrow terminal loses no wording</li><li><code>NO_KEY</code> and <code>DUMMY_KEY</code> no longer ask every language for every plural form. Korean needs only <code>item_other</code>, so <code>item_one</code> being absent from it was reported as missing and is not; Russian needs an <code>item_few</code> that English never writes, and that was reported as a stray key. Applies only to keys ending in a named plural category, and only to the languages the plural table covers</li><li><code>UNUSED_KEY</code> searches for a plural key by its base. No source file writes <code>item_one</code>, so every plural key in a project was reported as unused</li><li>The package now publishes <code>types</code>, so TypeScript consumers get the result shape</li><li>Analysis is linear in the number of keys rather than quadratic: comparing 5,000 keys across 5 locales went from about 10.8s to about 26ms</li><li>The twelve checks added since cost about a third as much again as the original twelve over the same 5,000 keys, and a check that is switched off costs nothing at all. <code>lengthRatio</code> is the exception worth knowing: measuring every value in display columns more than doubles the run, which is part of why it stays off until it is asked for</li></ul><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><code>--no-warn</code> hid the heading of a warning but still printed the lines under it. A suppressed issue is now left out entirely, and the report says how many it hid</li><li><code>EMPTY_VALUE</code> and <code>DUMMY_KEY</code> were written so their conditions could never be true, and never reported anything</li><li>A missing target language file threw instead of being reported</li><li>A relative path was joined as if it were absolute, so a scan rooted at one found nothing. Paths now resolve against the working directory</li><li>An explicit <code>path</code> option was overridden by the (absent) first argument, which is how the CLI passes it</li></ul><h2 id="_0-1-0-0-3-0-2026-04-14-2026-04-17-beta" tabindex="-1">0.1.0 ~ 0.3.0 (2026-04-14 ~ 2026-04-17) (Beta) <a class="header-anchor" href="#_0-1-0-0-3-0-2026-04-14-2026-04-17-beta" aria-label="Permalink to &quot;0.1.0 ~ 0.3.0 (2026-04-14 ~ 2026-04-17) (Beta)&quot;">​</a></h2><blockquote><p>This is for the Beta release. It can be used in production, but some features are still under development.</p></blockquote><ul><li>Cleanup and refactoring (0.1.0)</li><li>Improved error handling (0.1.0)</li><li>Fix and cleanup codes (0.2.0)</li><li>Add <code>NO_INTERPOLATION_KEY</code> check (0.2.0)</li><li>Base JS function support (0.3.0)</li></ul><h2 id="_0-0-1-0-0-5-2026-04-14-alpha" tabindex="-1">0.0.1 ~ 0.0.5 (2026-04-14) (Alpha) <a class="header-anchor" href="#_0-0-1-0-0-5-2026-04-14-alpha" aria-label="Permalink to &quot;0.0.1 ~ 0.0.5 (2026-04-14) (Alpha)&quot;">​</a></h2><blockquote><p>This is for the Alpha release and is not recommended for use</p></blockquote><ul><li>Initial release</li></ul>',24))]),o("div",h,[e[15]||(e[15]=o("p",null,[a("pub.dev의 "),o("code",null,"chki18n"),a(" 패키지입니다.")],-1)),e[16]||(e[16]=o("h2",{id:"_1-2-0-2026-09-05-1",tabindex:"-1"},[a("1.2.0 (2026-09-05) "),o("a",{class:"header-anchor",href:"#_1-2-0-2026-09-05-1","aria-label":'Permalink to "1.2.0 (2026-09-05)"'},"​")],-1)),e[17]||(e[17]=o("blockquote",null,[o("p",null,"A scan can be pointed at an application's root rather than at a folder of locales. It skips the configuration and lock files that root is full of, an exclude can name a path rather than a bare directory name, and it reports which interpolation delimiters the files it read appear to use.")],-1)),e[18]||(e[18]=o("h3",{id:"added-2",tabindex:"-1"},[a("Added "),o("a",{class:"header-anchor",href:"#added-2","aria-label":'Permalink to "Added"'},"​")],-1)),o("ul",null,[e[13]||(e[13]=t("<li><code>excludeFiles</code> names the files a scan never reads, as patterns where <code>*</code> stands for any run of characters and case is ignored. It defaults to the configuration and lock files no project keeps translations in — <code>package.json</code>, <code>tsconfig.json</code>, <code>tsconfig.*.json</code>, <code>eslintrc.json</code>, <code>*-lock.json</code>, <code>*-config.json</code> and <code>*.config.json</code> — which a scan of an application root used to read and parse in full on every run. The default list is exported as <code>defaultExcludeFiles</code>, and <code>--exclude-files</code> is the flag</li>",1)),o("li",null,[e[10]||(e[10]=o("code",null,"detectInterpolationDelimiters",-1)),e[11]||(e[11]=a(" guesses which delimiters a text writes its interpolation keys with: ",-1)),o("code",null,n(d.name),1),e[12]||(e[12]=t(" before <code>{name}</code>, and <code>[[ ]]</code>, <code>(( ))</code> and <code>&lt;&lt; &gt;&gt;</code> after them. <code>Chki18nScanResult.detectedInterpolation</code> reports what a whole scan saw, which <code>loadTranslations</code> carries as <code>session.detectedInterpolation</code>. It is a suggestion for a project being set up, never what the run compared with — that stays <code>interpolationPrefix</code>. The pairs it knows are exported in order as <code>interpolationDelimiters</code>",18))]),e[14]||(e[14]=o("li",null,[o("code",null,"createPathExcluder"),a(" and "),o("code",null,"createFileExcluder"),a(" build the two tests a scan applies, so an application that shows a user which folders are excluded can ask the same question the scan asks")],-1))]),e[19]||(e[19]=t('<h3 id="changed-3" tabindex="-1">Changed <a class="header-anchor" href="#changed-3" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><code>exclude</code> accepts a path as well as a name. One segment still names a directory at any depth, so <code>node_modules</code> means every <code>node_modules</code> there is; an entry with a separator names a path from the scanned root, so <code>src/legacy</code> excludes that folder and everything under it without touching a <code>legacy</code> belonging to something else</li><li>Both exclude lists apply to the source tree <code>source</code> names as well as to the translation directory</li></ul><h2 id="_1-1-0-2026-09-04-1" tabindex="-1">1.1.0 (2026-09-04) <a class="header-anchor" href="#_1-1-0-2026-09-04-1" aria-label="Permalink to &quot;1.1.0 (2026-09-04)&quot;">​</a></h2><blockquote><p>The target language is checked too. Everything else is compared against it, so it sat outside every check, and a mistake typed into the source language stayed there however often the files were checked.</p></blockquote><h3 id="changed-4" tabindex="-1">Changed <a class="header-anchor" href="#changed-4" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li>The target language is now checked by everything that reads one value on its own: <code>EMPTY_VALUE</code>, <code>SURROUNDING_WHITESPACE</code>, <code>INVISIBLE_CHARACTER</code>, <code>INVALID_VALUE_TYPE</code> and <code>UNTRANSLATED_SCRIPT</code>. It was skipped outright before, so an empty string, a trailing space or a zero width character in <code>en.json</code> was never reported. The comparison checks still say nothing about it, since it is what they compare against. None of the five reports at <code>error</code>, so a run that passed still passes; switch one off with <code>ignoreChecks</code> the way you would for any other language</li><li>An issue about the target language&#39;s own value carries no <code>targetValue</code>. What a report quotes beside a finding is the value it would be compared to, and for the target language that is the value already shown</li></ul><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li>The <code>github</code> reporter writes the annotation&#39;s <code>file=</code> path with forward slashes on every platform. On a Windows runner it wrote the platform&#39;s own separator, which GitHub matches against nothing, so the annotation silently attached to no file</li></ul><h2 id="_1-0-0-2026-09-03-1" tabindex="-1">1.0.0 (2026-09-03) <a class="header-anchor" href="#_1-0-0-2026-09-03-1" aria-label="Permalink to &quot;1.0.0 (2026-09-03)&quot;">​</a></h2><blockquote><p>The first release. A port of the JavaScript package at the same version: the same twenty-five checks, the same option names, the same report, and the same exit code from the command line.</p></blockquote><h3 id="added-3" tabindex="-1">Added <a class="header-anchor" href="#added-3" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><code>checkTranslationFiles</code> reads a directory of translation files and compares every language against the target language, in one call</li><li><code>analyzeTranslations</code> compares translations passed in directly, with no file system work</li><li><code>createAnalyzer</code> returns a reusable analyzer whose <code>checkEntry</code> re-checks a single key</li><li><code>loadTranslations</code> reads a directory once and returns a session that holds the parsed translations: <code>analyze</code>, <code>checkKey</code>, <code>get</code>, <code>set</code>, <code>remove</code>, <code>keys</code>, <code>translations</code> and <code>reload</code> all work on what is already in memory. <code>createSession</code> is the same for translations passed in directly</li><li>Twenty-five checks. <code>NO_KEY</code>, <code>NO_LOCALE</code>, <code>DUPLICATE_KEY</code>, <code>NO_INTERPOLATION_KEY</code>, <code>EXTRA_INTERPOLATION_KEY</code> and <code>INTERPOLATION_COUNT</code> report at <code>error</code> and fail a run; <code>DUMMY_KEY</code>, <code>EMPTY_VALUE</code>, <code>INVALID_VALUE_TYPE</code>, <code>TAG_MISMATCH</code>, <code>NOT_TRANSLATED_VALUE</code>, <code>UNTRANSLATED_SCRIPT</code>, <code>DUPLICATE_VALUE</code>, <code>INCONSISTENT_VALUE</code>, <code>SURROUNDING_WHITESPACE</code>, <code>INVISIBLE_CHARACTER</code>, <code>MISSING_NUMBER</code>, <code>NUMBER_MISMATCH</code>, <code>NO_PLURAL_FORM</code>, <code>KEY_NAMING</code>, <code>KEY_DEPTH</code> and <code>UNDEFINED_KEY</code> at <code>warn</code>; <code>UNUSED_KEY</code> and <code>SUSPICIOUS_LENGTH</code> at <code>info</code></li><li>Every on-disk layout: one file per locale (<code>en.json</code>), one folder per locale (<code>en/common.json</code>), and one file holding every locale (<code>{&quot;en&quot;: ...}</code>). Files holding the same keys are compared as a group</li><li>Five reporters — <code>pretty</code>, <code>list</code>, <code>json</code>, <code>markdown</code> and <code>github</code> — and five grouping axes: <code>locale</code>, <code>code</code>, <code>group</code>, <code>file</code> and <code>none</code>. <code>output</code> writes a copy to a file, in the shape its extension implies</li><li>The <code>chki18n</code> command, which takes the same options as the library and exits with <code>1</code> when an <code>error</code> level issue was found. Install it with <code>dart pub global activate chki18n</code></li><li><code>package:chki18n/core.dart</code>, the comparison on its own. It imports no <code>dart:io</code>, so it runs in a Flutter web build or anywhere else the file system is not there</li></ul><h3 id="notes-on-the-port" tabindex="-1">Notes on the port <a class="header-anchor" href="#notes-on-the-port" aria-label="Permalink to &quot;Notes on the port&quot;">​</a></h3><ul><li>Options are one object with named parameters — <code>Chki18nOptions(target: &#39;en&#39;)</code> — rather than the twenty-four named parameters each entry point would otherwise carry. Every field is optional and every one has the JavaScript default</li><li>Enums replace the string unions: <code>Chki18nCheckCode.noKey</code>, <code>Chki18nLevel.error</code>, <code>Chki18nFileFormat.single</code>. Each carries the spelling the CLI and the JSON reporter share, so <code>Chki18nCheckCode.noKey.code</code> is <code>NO_KEY</code> in every package</li><li>The loose text forms a command line writes — <code>&#39;NO_KEY,EMPTY_VALUE&#39;</code>, <code>&#39;EMPTY_VALUE=error&#39;</code> — live on <code>Chki18nTextOptions</code> rather than being a second accepted type on every field. <code>resolveOptions</code> reads both, and a typed field wins over the text one</li><li>A translation file whose JSON is not an object is reported as <code>INVALID_FILE</code> rather than read as an empty one</li><li>No dependencies. The argument parser, the ANSI colours and the path helpers are written out, because <code>args</code>, <code>chalk</code> and <code>package:path</code> would be the only three</li></ul>',14))]),o("div",u,[e[25]||(e[25]=o("p",null,[a("PyPI의 "),o("code",null,"chki18n"),a(" 패키지입니다.")],-1)),e[26]||(e[26]=o("h2",{id:"_1-2-0-2026-09-05-2",tabindex:"-1"},[a("1.2.0 (2026-09-05) "),o("a",{class:"header-anchor",href:"#_1-2-0-2026-09-05-2","aria-label":'Permalink to "1.2.0 (2026-09-05)"'},"​")],-1)),e[27]||(e[27]=o("blockquote",null,[o("p",null,"A scan can be pointed at an application's root rather than at a folder of locales. It skips the configuration and lock files that root is full of, an exclude can name a path rather than a bare directory name, and it reports which interpolation delimiters the files it read appear to use.")],-1)),e[28]||(e[28]=o("h3",{id:"added-4",tabindex:"-1"},[a("Added "),o("a",{class:"header-anchor",href:"#added-4","aria-label":'Permalink to "Added"'},"​")],-1)),o("ul",null,[e[23]||(e[23]=t("<li><code>exclude_files</code> names the files a scan never reads, as patterns where <code>*</code> stands for any run of characters and case is ignored. It defaults to the configuration and lock files no project keeps translations in — <code>package.json</code>, <code>tsconfig.json</code>, <code>tsconfig.*.json</code>, <code>eslintrc.json</code>, <code>*-lock.json</code>, <code>*-config.json</code> and <code>*.config.json</code> — which a scan of an application root used to read and parse in full on every run. The default list is exported as <code>DEFAULT_EXCLUDE_FILES</code>, and <code>--exclude-files</code> is the flag</li>",1)),o("li",null,[e[20]||(e[20]=o("code",null,"detect_interpolation_delimiters",-1)),e[21]||(e[21]=a(" guesses which delimiters a text writes its interpolation keys with: ",-1)),o("code",null,n(d.name),1),e[22]||(e[22]=t(" before <code>{name}</code>, and <code>[[ ]]</code>, <code>(( ))</code> and <code>&lt;&lt; &gt;&gt;</code> after them. <code>ScanResult.detected_interpolation</code> reports what a whole scan saw, which <code>load_translations</code> carries as <code>session.detected_interpolation</code>. It is a suggestion for a project being set up, never what the run compared with — that stays <code>interpolation_prefix</code>. The pairs it knows are exported in order as <code>INTERPOLATION_DELIMITERS</code>",18))]),e[24]||(e[24]=o("li",null,[o("code",null,"create_path_excluder"),a(" and "),o("code",null,"create_file_excluder"),a(" build the two tests a scan applies, so an application that shows a user which folders are excluded can ask the same question the scan asks")],-1))]),e[29]||(e[29]=t('<h3 id="changed-5" tabindex="-1">Changed <a class="header-anchor" href="#changed-5" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><code>exclude</code> accepts a path as well as a name. One segment still names a directory at any depth, so <code>node_modules</code> means every <code>node_modules</code> there is; an entry with a separator names a path from the scanned root, so <code>src/legacy</code> excludes that folder and everything under it without touching a <code>legacy</code> belonging to something else</li><li>Both exclude lists apply to the source tree <code>source</code> names as well as to the translation directory</li></ul><h2 id="_1-1-0-2026-09-04-2" tabindex="-1">1.1.0 (2026-09-04) <a class="header-anchor" href="#_1-1-0-2026-09-04-2" aria-label="Permalink to &quot;1.1.0 (2026-09-04)&quot;">​</a></h2><blockquote><p>The target language is checked too. Everything else is compared against it, so it sat outside every check, and a mistake typed into the source language stayed there however often the files were checked.</p></blockquote><h3 id="changed-6" tabindex="-1">Changed <a class="header-anchor" href="#changed-6" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li>The target language is now checked by everything that reads one value on its own: <code>EMPTY_VALUE</code>, <code>SURROUNDING_WHITESPACE</code>, <code>INVISIBLE_CHARACTER</code>, <code>INVALID_VALUE_TYPE</code> and <code>UNTRANSLATED_SCRIPT</code>. It was skipped outright before, so an empty string, a trailing space or a zero width character in <code>en.json</code> was never reported. The comparison checks still say nothing about it, since it is what they compare against. None of the five reports at <code>error</code>, so a run that passed still passes; switch one off with <code>ignore_checks</code> the way you would for any other language</li><li>An issue about the target language&#39;s own value carries no <code>target_value</code>. What a report quotes beside a finding is the value it would be compared to, and for the target language that is the value already shown</li></ul><h3 id="fixed-3" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-3" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li>The <code>github</code> reporter writes the annotation&#39;s <code>file=</code> path with forward slashes on every platform. On a Windows runner it wrote the platform&#39;s own separator, which GitHub matches against nothing, so the annotation silently attached to no file</li><li>The command writes UTF-8 to the console whatever its code page is. On Windows it printed the banner and then died with a <code>UnicodeEncodeError</code> half a report in, exiting <code>1</code> on a directory with nothing wrong</li></ul><h2 id="_1-0-0-2026-09-03-2" tabindex="-1">1.0.0 (2026-09-03) <a class="header-anchor" href="#_1-0-0-2026-09-03-2" aria-label="Permalink to &quot;1.0.0 (2026-09-03)&quot;">​</a></h2><blockquote><p>The first release. A port of the JavaScript package at the same version: the same twenty-five checks, the same option names, the same report, and the same exit code from the command line.</p></blockquote><h3 id="added-5" tabindex="-1">Added <a class="header-anchor" href="#added-5" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><code>check_translation_files</code> reads a directory of translation files and compares every language against the target language, in one call</li><li><code>analyze_translations</code> compares translations passed in directly, with no file system work</li><li><code>create_analyzer</code> returns a reusable analyzer whose <code>check_entry</code> re-checks a single key</li><li><code>load_translations</code> reads a directory once and returns a session that holds the parsed translations: <code>analyze</code>, <code>check_key</code>, <code>get</code>, <code>set</code>, <code>remove</code>, <code>keys</code>, <code>translations</code> and <code>reload</code> all work on what is already in memory. <code>create_session</code> is the same for translations passed in directly</li><li>Twenty-five checks. <code>NO_KEY</code>, <code>NO_LOCALE</code>, <code>DUPLICATE_KEY</code>, <code>NO_INTERPOLATION_KEY</code>, <code>EXTRA_INTERPOLATION_KEY</code> and <code>INTERPOLATION_COUNT</code> report at <code>error</code> and fail a run; <code>DUMMY_KEY</code>, <code>EMPTY_VALUE</code>, <code>INVALID_VALUE_TYPE</code>, <code>TAG_MISMATCH</code>, <code>NOT_TRANSLATED_VALUE</code>, <code>UNTRANSLATED_SCRIPT</code>, <code>DUPLICATE_VALUE</code>, <code>INCONSISTENT_VALUE</code>, <code>SURROUNDING_WHITESPACE</code>, <code>INVISIBLE_CHARACTER</code>, <code>MISSING_NUMBER</code>, <code>NUMBER_MISMATCH</code>, <code>NO_PLURAL_FORM</code>, <code>KEY_NAMING</code>, <code>KEY_DEPTH</code> and <code>UNDEFINED_KEY</code> at <code>warn</code>; <code>UNUSED_KEY</code> and <code>SUSPICIOUS_LENGTH</code> at <code>info</code></li><li>Every on-disk layout: one file per locale (<code>en.json</code>), one folder per locale (<code>en/common.json</code>), and one file holding every locale (<code>{&quot;en&quot;: ...}</code>). Files holding the same keys are compared as a group</li><li>Five reporters — <code>pretty</code>, <code>list</code>, <code>json</code>, <code>markdown</code> and <code>github</code> — and five grouping axes: <code>locale</code>, <code>code</code>, <code>group</code>, <code>file</code> and <code>none</code>. <code>output</code> writes a copy to a file, in the shape its extension implies</li><li>The <code>chki18n</code> command, which takes the same options as the library and exits with <code>1</code> when an <code>error</code> level issue was found</li><li><code>chki18n.core</code>, the comparison on its own. It reaches no file system, so an application that must not touch the disk can import the checks without the scanner</li></ul><h3 id="notes-on-the-port-1" tabindex="-1">Notes on the port <a class="header-anchor" href="#notes-on-the-port-1" aria-label="Permalink to &quot;Notes on the port&quot;">​</a></h3><ul><li>Options are one keyword-only frozen dataclass — <code>Options(target=&quot;en&quot;)</code> — rather than the twenty-four keyword arguments each entry point would otherwise carry. Every field is optional and every one has the JavaScript default</li><li>Check codes, severities and the option choices are the same strings the JavaScript package uses, typed as <code>Literal</code>, so <code>&quot;NO_KEY&quot;</code> and <code>&quot;error&quot;</code> need no translating between the two. <code>checks</code> accepts a list or the comma separated text a flag gives it, and <code>levels</code> a mapping or the CLI&#39;s <code>CODE=level</code> list</li><li>The entry points are synchronous. The JavaScript package is asynchronous because Node&#39;s file system is; Python&#39;s is not, and an <code>async</code> surface would be a promise this package cannot keep anything with</li><li>A translation file whose JSON is not an object is reported as <code>INVALID_FILE</code> rather than read as an empty one</li><li>No dependencies. The argument parser, the ANSI colours and the JSON key scanner are written out, because <code>argparse</code> cannot express <code>--no-x</code> as the negation of <code>x</code> and the other two would be a dependency each</li></ul>',14))])])}const _=i(c,[["render",p]]);export{k as __pageData,_ as default};
